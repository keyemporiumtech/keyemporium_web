import { DdcDateValidationDirective } from './ddc-date-validation.directive';

describe('DdcDateValidationDirective', () => {
	it('should create an instance', () => {
		const directive = new DdcDateValidationDirective();
		expect(directive).toBeTruthy();
	});
});
